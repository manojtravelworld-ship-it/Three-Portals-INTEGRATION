import React from 'react';

/**
 * The VoiceInterface logic is now primarily handled by the remote agent 
 * at the provided URL to ensure that any updates to the website 
 * reflect automatically in this PWA.
 */
export const VoiceInterface: React.FC<any> = () => {
  return null;
};
